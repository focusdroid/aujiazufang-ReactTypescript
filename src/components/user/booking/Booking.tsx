import React, { Fragment } from 'react'

import TitleBar from "../../../plugin/titleBar/TitleBar";

const Booking = () => {
    return (<Fragment>
        <TitleBar title={'预定'}/>
        <h3>预定</h3>
    </Fragment>)
}

export default Booking