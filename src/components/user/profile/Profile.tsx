import React, { Fragment } from 'react'

import TitleBar from "../../../plugin/titleBar/TitleBar";

const Profile = () => {
    return (<Fragment>
        <TitleBar title={'个人信息'}/>
        <h2>个人信息</h2>
    </Fragment>)
}

export default Profile