import React, { Fragment } from 'react'
const logo = require('../../style/less/base.module.less')

const Logo = () => {
    return (<Fragment>
        <div className={logo.title}>iHome</div>
    </Fragment>)
}
export default Logo